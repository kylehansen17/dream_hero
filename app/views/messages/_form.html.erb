<%= simple_form_for [chat, message] do |f| %>
  <%= f.input :content, label: "Ask a question", required: true, placeholder: "Which path do you take?" %>
  <%= f.button :submit, 'Go', class: 'btn btn-primary' %>
<% end %>
